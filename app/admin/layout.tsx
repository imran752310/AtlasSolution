export const metadata = {
  title: "Admin Dashboard | Nexora Digital",
  description: "Manage project submissions and contact messages",
}

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
